import React from 'react';
import { View, Text } from 'react-native';

// Legacy placeholder screen kept for navigation compatibility.
// This component simply renders a placeholder to keep the navigation working.
export const AdminDashboard = () => (
  <View>
    <Text>Admin dashboard (legacy)</Text>
  </View>
);
